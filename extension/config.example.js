// Template for extension/config.js (which is gitignored).
// Copy this file to config.js and set API_KEY to match the server's API_KEY env var.
// Leave it as '' to run without an API key.
self.API_KEY = '';
